Ejercicio sintético. Dado el esquema Alumno(id, nombre) e Inscripcion(alumno_id, materia), escriba una consulta SQL que liste los identificadores de estudiantes inscriptos.

```sql
SELECT alumno_id
FROM Inscripcion;
```
